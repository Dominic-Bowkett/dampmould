---
/**
 * CompensationCalculator — client-side island (lead magnet on tenant pages).
 *
 * SCAFFOLD STUB. Per CLAUDE.md build order, the working calculator logic is
 * built in step 7 alongside Pillar B. This placeholder establishes the
 * component + island wiring so the build is complete; the indicative-range
 * formula and disclaimer copy are intentionally left as a TODO for that step.
 */
---

<section class="calc card" aria-labelledby="calc-h">
  <h2 id="calc-h">Damp &amp; mould compensation calculator</h2>
  <p class="calc-note">
    <!-- TODO (build step 7): inputs = monthly rent, months affected, severity,
         whether reported to landlord. Output = indicative range + "not legal
         advice" disclaimer + CTA to the referral LeadForm. Pure client-side JS. -->
    Calculator coming soon.
  </p>
</section>

<style>
  .calc { margin-block: var(--space-12); }
  .calc h2 { margin-top: 0; }
  .calc-note { color: var(--color-muted); }
</style>
